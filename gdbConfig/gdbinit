# 设置 list 命令一次显示的源代码行数，默认10行，这里设为30行
set listsize 30

# 设置打印字符串或数组时显示的最大元素数量，0表示无限制（默认为200）
set print elements 0

# 以易于阅读的缩进格式显示结构体
set print pretty on

# 打印数组时，每个元素单独占一行（默认为逗号分隔）
set print array on

# 打印数组时，显示每个元素的索引下标
set print array-indexes on

# 显示字符串时，遇到空字符则停止（对于C风格字符串很有用）
set print null-stop on
